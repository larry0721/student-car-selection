# types

Shared TypeScript contracts.

- `buyer.ts`: questionnaire and affordability profile types.
- `vehicle.ts`: vehicle catalog, scored recommendation, and AI personalization types.
- `data.ts`: normalized external data, CSV import, and provider overlay types.

Update these types before changing matching logic or API payloads so the rest of the app gets useful compiler feedback.
